% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finetune.R
\name{conflibert_compare}
\alias{conflibert_compare}
\title{Compare Multiple Models}
\usage{
conflibert_compare(
  train,
  dev,
  test,
  models = c("ConfliBERT", "BERT Base Uncased"),
  task = "binary",
  epochs = 3,
  batch_size = 8,
  lr = 2e-05,
  use_lora = FALSE,
  lora_rank = 8,
  lora_alpha = 16
)
}
\arguments{
\item{train}{A data.frame with \code{text} and \code{label} columns (training set).}

\item{dev}{A data.frame with \code{text} and \code{label} columns (validation set).}

\item{test}{A data.frame with \code{text} and \code{label} columns (test set).}

\item{models}{Character vector of model names to compare.
See \code{\link{conflibert_models}} for available names.}

\item{task}{\code{"binary"} or \code{"multiclass"}. Default: \code{"binary"}.}

\item{epochs}{Number of training epochs. Default: 3.}

\item{batch_size}{Training batch size. Default: 8.}

\item{lr}{Learning rate. Default: 2e-5.}

\item{use_lora}{If \code{TRUE}, fine-tune with a LoRA adapter
(parameter-efficient; cuts GPU memory roughly 5x). The adapter is merged
into the base model before saving, so loading is the same as for
full fine-tuning. Default: \code{FALSE}.}

\item{lora_rank}{LoRA rank. Default: 8.}

\item{lora_alpha}{LoRA alpha. Default: 16.}
}
\value{
A tibble with one row per model and columns for each metric
plus \code{runtime}. It has a themed \code{print()} method that
ranks models, and a \code{plot()} method comparing metrics.
}
\description{
Fine-tune several base models on the same dataset and return a
comparison table of test-set metrics. Useful for selecting the best
architecture for your data.
}
\examples{
\dontrun{
comparison <- conflibert_compare(
  train  = train_df,
  dev    = dev_df,
  test   = test_df,
  models = c("ConfliBERT", "BERT Base Uncased", "RoBERTa Base"),
  task   = "binary",
  epochs = 3
)
comparison
}
}
