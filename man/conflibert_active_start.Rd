% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/active.R
\name{conflibert_active_start}
\alias{conflibert_active_start}
\title{Start an Active Learning Session}
\usage{
conflibert_active_start(
  seed,
  pool,
  dev = NULL,
  model = "ConfliBERT",
  task = c("binary", "multiclass"),
  strategy = c("entropy", "margin", "least_confidence"),
  diverse = FALSE,
  diversity_candidates = NULL,
  query_size = 10,
  epochs = 3,
  batch_size = 8,
  lr = 2e-05,
  max_seq_len = 512,
  use_lora = FALSE,
  lora_rank = 8,
  lora_alpha = 16
)
}
\arguments{
\item{seed}{Labeled starter set. A data.frame with \code{text} and
\code{label} columns.}

\item{pool}{Unlabeled pool. Either a character vector of texts, or a
data.frame with a \code{text} column (other columns are ignored).}

\item{dev}{Optional validation set with \code{text} and \code{label}
columns. When provided, metrics are recorded each round.}

\item{model}{Base model. See \code{\link{conflibert_models}}.
Default: \code{"ConfliBERT"}.}

\item{task}{\code{"binary"} or \code{"multiclass"}. Default:
\code{"binary"}.}

\item{strategy}{Uncertainty strategy: \code{"entropy"} (default),
\code{"margin"}, or \code{"least_confidence"}.}

\item{diverse}{If \code{TRUE}, cluster the top-scoring candidates in
embedding space and pick the highest-scoring sample from each
cluster. Prevents near-duplicates from dominating a batch.
Default: \code{FALSE}.}

\item{diversity_candidates}{How many top-scoring candidates to
cluster when \code{diverse = TRUE}. Default: \code{3 * query_size}.}

\item{query_size}{Samples queried per round. Default: 10.}

\item{epochs}{Training epochs per round. Default: 3.}

\item{batch_size}{Training batch size. Default: 8.}

\item{lr}{Learning rate. Default: 2e-5.}

\item{max_seq_len}{Max token sequence length. Default: 512.}

\item{use_lora}{If \code{TRUE}, train each round with a LoRA adapter
(parameter-efficient). Default: \code{FALSE}.}

\item{lora_rank}{LoRA rank. Default: 8.}

\item{lora_alpha}{LoRA alpha. Default: 16.}
}
\value{
An object of class \code{"conflibert_al_session"}: a list
with \code{query} (tibble of texts to label), \code{metrics}
(tibble of metrics across rounds), \code{round}, \code{labeled_n},
\code{pool_n}, \code{done}, and internal state.
}
\description{
Train a classifier on a small labeled seed and select the most
uncertain samples from an unlabeled pool for you to label. The
returned session object tracks the labeled set, the pool, the
current query, and metrics across rounds.
}
\details{
The typical workflow is:
\enumerate{
\item Call \code{conflibert_active_start()} with your seed and pool.
\item Inspect \code{session$query} and assign labels.
\item Pass the labels to \code{\link{conflibert_active_next}} to
retrain and query the next batch.
\item Repeat until the pool is exhausted or metrics plateau.
\item Persist with \code{\link{conflibert_active_save}}.
}
}
\examples{
\dontrun{
seed <- data.frame(
  text  = c("Troops advanced.", "Nice weather today."),
  label = c(1L, 0L)
)
pool <- c("Car bomb exploded.", "New coffee shop opened.", "...")

session <- conflibert_active_start(
  seed = seed, pool = pool, query_size = 5, epochs = 1
)
session
session$query
}
}
